# CLAUDE.md

이 리포에서 작업할 때 반드시 지켜야 할 규칙이다.

## 프로젝트 개요

- 이 리포는 2D 메트로배니아 게임의 **상세 기획서(GDD)를 작성하는 문서 전용 워크스페이스**다.
- 게임 코드는 이 리포에 두지 않는다. 문서(Markdown)만 관리한다.
- 구현 대상 환경은 **Unity 6 / C# / PC**를 전제로 한다. 모든 문서는 이 전제로 작성한다.
- 대화와 문서는 모두 **한국어**로 작성한다.

## 기준 문서

- `reference/textbook.pdf`가 이 프로젝트의 **유일한 기준 교재**다.
- 문서 작성 원칙은 `reference/textbook_rules.md`를 따른다.
- 문서 구조는 `reference/templates/`의 템플릿을 따른다.
- 기획서는 아이디어 메모가 아니라, 사람과 AI가 함께 읽는 **구현 명세서**다. (교재 Ch.7~8)

## 산출물 위치

| 위치 | 내용 |
|---|---|
| `docs/` | 게임 상세 기획서 본문 |
| `docs/99_change_log.md` | 기획 변경 이력 (한 줄 단위) |
| `reference/textbook_rules.md` | 교재 요약과 작성 원칙 |
| `reference/templates/` | 교재 부록 기반 문서 템플릿 |

## docs/ 파일명 규칙 (고정)

docs/ 아래 파일명은 아래 목록으로 **고정**한다. 새 이름이 필요하면 만들기 전에 먼저 사용자에게 묻는다.

```
00_game_concept    01_core_loop       02_player_control  03_combat_system
04_ability_gate    05_map_progression 06_item_reward     07_ui_flow
08_stage_design    09_data_table      99_change_log
```

## 문서 작성 규칙

- 한국어로, **짧고 명확한 문장**으로 쓴다. (교재 Ch.8)
- 하나의 기능은 **목적 / 조건 / 처리 흐름 / 예외**로 분해해서 쓴다. (교재 Ch.8, Ch.90)
- "적당히", "자연스럽게", "알아서", "좋은 느낌으로" 같은 **모호한 표현을 금지**한다.
  구현 가능한 조건 문장으로 바꿔 쓴다. (교재 Ch.90)
- **예외 조건은 필수**다. 정상 흐름만 쓴 문서는 미완성이다.
  실패, 자원 부족, 중복 처리, 비정상 입력까지 정의한다. (교재 Ch.25, Ch.81)
- 능력치, 확률, 쿨타임 등 **수치는 표로** 정리한다. (교재 Ch.17, Part 9)
- 각 문서 끝에 **"AI 구현 요청 프롬프트"** 절을 둔다.
  구조는 목적 → 환경 → 요구 기능 → 데이터 구조 → 처리 흐름 → 예외 조건 → 출력 형식. (교재 Ch.91)

## 금지 사항

- **정하지 않은 값을 임의로 채우지 않는다.** 수치, 규칙, 이름 등 사용자가 정하지 않은 것은 추측하지 않는다.
- 95% 확신이 없으면 문서를 쓰기 전에 **질문한다**. 질문은 한 번에 **3개 이하**.
- **요청받은 문서만** 만든다. 요청 없이 다른 문서를 만들거나 고치지 않는다.
- 추측으로 답하지 않는다. 교재나 기존 문서에서 근거를 찾은 뒤 답한다.
- 기존 파일 삭제나 전면 재작성은 **사용자 승인 후에만** 한다.
- 사용자가 교재 범위를 벗어난 포괄적 질문을 하면, 바로 답하지 말고
  **교재의 어느 항목(Part/Chapter)이 필요한지 되묻는다**.

## 작업 흐름

1. 문서는 **한 번에 하나씩** 작성한다. 한 번에 여러 문서를 완성하려 하지 않는다. (교재 Ch.111)
2. 작성 전: 해당 템플릿을 열어 **아직 정해지지 않은 항목을 질문 목록으로 제시**하고, 답을 받은 뒤 작성한다.
3. 작성 후: 교재 부록 J 체크리스트로 자체 검토하고, 결과를 **3~5줄로 보고**한다.
4. 문서를 변경하면 `docs/99_change_log.md`에 **한 줄** 기록한다. (교재 Ch.112~113)

## 모델 역할 분담: Advisor / Worker

너는 Advisor다. 판단에 집중하고, 구현 노동은 Worker에게 위임하라.
이 리포에서 "구현"은 문서 작성·수정 노동을 뜻한다.

### Advisor가 직접 하는 일

- 요구사항 분석, 작업 분해, 설계 결정
- Worker에게 줄 작업 브리프 작성
- 결과 검증: diff 직접 확인, 교재·기존 문서와 직접 대조
- 최종 커밋 승인, 사용자 보고

### Worker(서브에이전트)에게 위임하는 일

- 문서 작성과 수정 등 구현 작업 전부
- Agent 도구로 위임하고 model은 "opus 5"(high)를 지정한다
- 서로 독립적인 작업은 병렬로 위임한다

### 브리프 기준

- Advisor가 이미 파악한 컨텍스트를 담아 Worker가 재탐색하지 않게 하라
- 파일 경로, 프로젝트 컨벤션(이 CLAUDE.md의 규칙), 알려진 함정,
  완료 기준(교재 근거, 줄 수 제한 등)을 포함하라

### 경계

- Worker의 완료 보고를 그대로 믿지 마라. diff와 원문 대조로 직접 확인한 뒤 승인하라
- 검증 실패는 수정 브리프로 재위임하라. 직접 수정은 사소한 마무리에만 허용된다
- 한두 줄 수정처럼 위임 오버헤드가 더 큰 작업은 직접 처리해도 된다

## 커밋 규칙

- 커밋 메시지에는 "문서 수정"이 아니라 **변경 이유**를 쓴다.
  예: "상점 구매 실패 조건 추가", "보스 페이즈 전환 조건 변경". (교재 Ch.114)
- 코드가 없는 리포이므로, 기획 규칙이 바뀐 이유가 커밋 이력에 남아야 한다.
